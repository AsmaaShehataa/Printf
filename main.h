#ifndef MAIN_H
#define MAIN_H

#include <limits.h>
#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>

/** typedef - struct printmystrwrite
{
	char j;
	int(*j(va_list(args, specifier *j)));
}specifier_handler;

typedef - struct ourflags
{
	int +;
	int space;
	int #
}ourflags_HA;
*/
int _printf(const char *format, ...);
int printmystrwrite(char str[]);
int printmycharwrite(char charro);
char convertdigit(int x);
int mynumlen(int num1);
char *convertnumbertocharz(int number1);

#endif
